#include <libnet/mc/item.h>

namespace mc
{
namespace server
{

//std::function<size_t(std::string)> Item::hash_func= std::hash<std::string>();
  // void Item::set_key(const char* key, uint8_t len)
  // {
  //   ::memcpy(data_, key, len);
  //   data_[len] = '\0';
  //   key_size_ = len;
    //assert(Item::hash_func_);
    //hash_ = std::hash<std::string>()(std::string(data_, len));//Item::hash_func_(std::string(data_, len));
  //};
 
  //void Item::set_hash_func(HashFunc func)
  //{
    //Item::hash_func_ = func;
  //};
}
}